import org.junit.Test;

import static org.junit.Assert.*;
import static org.mockito.Matchers.anyString;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class SetupTest {
//https://stackoverflow.com/questions/6415728/junit-testing-with-simulated-user-input
    //https://github.com/mockito/mockito/wiki/Features-And-Motivations
 //   @Test
 //   public void getPlayerCount_onlyOne(){
 //       Querist querist = mock(Querist.class);
 //       when(querist.ask(anyString())).thenReturn(1);
 //       Setup Setup = new Setup();
    //       assertEquals(Setup.getPlayerCount(querist),2);
    //   }

    // Test if correct ShipType in method getShipType

    // Test if Map has the identical shipname twice (map.putifabsent)
}